import { ObserveSection } from './observe-section';

describe('ObserveSection', () => {
  it('should create an instance', () => {
    const directive = new ObserveSection();
    expect(directive).toBeTruthy();
  });
});
